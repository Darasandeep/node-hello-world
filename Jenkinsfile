pipeline {
    agent {
        ansiColor('xterm') {
            dockerfile true
        }
    }

    stages {
        stage('Foo') {
            steps {
                echo 'Hello'
            }
        }
    }
}
